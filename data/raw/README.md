Raw datasets are downloaded automatically by `scripts/prepare_data.py`
into `data/processed/` as JSONL files.

This directory (`data/raw/`) is reserved for any manually downloaded
or cached files (e.g. HuggingFace cache, custom splits).

Do NOT commit large dataset files to version control.
